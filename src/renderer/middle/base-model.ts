import { injectable } from 'inversify'
// import {Barrier} from "../../common/barrier";


@injectable()
export class BaseToElectron {
  // _initWait: Barrier<boolean>
  //
  // constructor() {
  //   this._initWait = new Barrier<boolean>()
  // }
  //
  // protected async init() {
  //   await this._initWait.pass(true)
  // }
  //
  // protected async awaitInit() {
  //   await this._initWait.wait()
  // }
}
